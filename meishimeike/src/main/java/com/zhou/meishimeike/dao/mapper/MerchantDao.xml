<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhou.meishimeike.dao.MerchantDao">
	<select id="hasUser" resultType="boolean">
			select count(m_id) from user where u_phone=#{param1}  and u_pass=#{param2}
		</select>
		
		<insert id="addMerchant">
			INSERT INTO merchant(m_phone,m_pass) values(#{param1},#{param2})
		</insert> 
</mapper>