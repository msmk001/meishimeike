<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhou.meishimeike.dao.OrderDao">

	<!-- private Integer oId	;
	private Integer mId	;
	private Integer uId	;
	
	private Integer oCode; //1 未支付  2已经支付
	private String oTradeNo;
	private String oInfo;
	
	private Double oPrice; -->
	
	
	<insert id="addOrder">
		insert into order_form values(default , #{mId}, #{uId}, #{oTradeNo}, #{oPrice}, default, #{oInfo},now(),#{ufId})
	</insert>
	
	<update id="updateOrderCode">
		 
	</update>
</mapper>